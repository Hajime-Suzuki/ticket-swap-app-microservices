{
  "name": "local",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "docker-compose up --build -d",
    "subscribe": "python3 setup.py",
    "logs": "docker-compose logs -t",
    "deploy": "serverless deploy -v"
  },
  "devDependencies": {
    "serverless": "^1.48.2",
    "serverless-offline-sns": "^0.53.0",
    "serverless-pseudo-parameters": "^2.4.0"
  }
}
