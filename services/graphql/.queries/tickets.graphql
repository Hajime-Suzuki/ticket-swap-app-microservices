# for developing purpose

query getTicket {
  getTicket(eventId: "124", userId: "1") {
    ticket {
      userId
      createdAt
      price
    }
  }
}

mutation createTicket {
  createTicket(data: { userId: "1", eventId: "124", price: "15.09" }) {
    userId
  }
}
