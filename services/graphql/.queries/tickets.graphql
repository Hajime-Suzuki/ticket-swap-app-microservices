# for developing purpose

query getTickets {
  getTickets(args: { eventId: "100" }) {
    tickets {
      id
      eventId
      price
      date
    }
  }
}

mutation createTicket {
  createTicket(
    data: { userId: "1", eventId: "124", price: "15.09", date: "2019-08-15" }
  ) {
    userId
  }
}
